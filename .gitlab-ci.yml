stages:
  - lint
  - test

lint:
  stage: lint

  before_script:
    - apt-get update -qq && apt-get install -y -qq flake8

  script: flake8

test:
  stage: test

  before_script:
    - pip install pytest pytest-django

  script:
    - py.test